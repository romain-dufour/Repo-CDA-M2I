import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function DetailsPokemon(name,url) {
console.log(url);
  return (
    <View>
      {/* <Text>{name}</Text> */}
      <Text>toto</Text>
    </View>
  )
}

const styles = StyleSheet.create({})